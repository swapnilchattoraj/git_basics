git config --global user.name "Swapnil Chattoraj"
git config --global user.email "swap@gmail.com"
git config user.name

git status -h / git status --help
cd /c/users/swp/desktop/folder
git config --global core.editor "notepad"
got config --global core.editor "vi"

git init
git add abc.txt xyz.txt
git status
git status -s
git add *.java
git add *.txt
git commit -m "init commit"
git commit -a -m "new changes"

git rm --cached .classpath (to make untrack from git)
git status
git rm -r --cached bin/ (folder)
git status

touch .gitignore (file)
nano .gitignore (read/write)
(
*.class
bin/
.classpath
)

git status
git add . (to add all the file)
git commit -m "added .gitignore and remove bin/ from staging"
git log (commit logs)
git log -2 (last two logs)

touch abc.txt
git add .

git restore --staged src/helloworld.java
git status













